import { ApplyClassOnHoverDirective } from './apply-class-on-hover.directive';

describe('ApplyClassOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyClassOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
